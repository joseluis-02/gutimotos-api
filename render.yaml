services:
  - type: web
    name: gutimotos-api-dev
    env: python
    buildCommand: "./build.sh"  # Se ejecutará el script de construcción
    startCommand: "poetry run gunicorn src.config.wsgi:application --workers 3 --preload"  # Comando de inicio
    rootDirectory: src
    autoDeploy: true